apiVersion: v1
kind: Service
metadata:
  name: {{ include "context-bridge.fullname" . }}
  labels:
    {{- include "context-bridge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "context-bridge.selectorLabels" . | nindent 4 }}
