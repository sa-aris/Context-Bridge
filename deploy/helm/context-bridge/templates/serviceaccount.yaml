{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "context-bridge.serviceAccountName" . }}
  labels:
    {{- include "context-bridge.labels" . | nindent 4 }}
{{- end }}
